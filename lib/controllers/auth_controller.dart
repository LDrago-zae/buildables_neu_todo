class AuthController {
  Future<void> login(String email, String password) async {
    // Placeholder for future auth logic (API, validation, etc.)
    await Future<void>.delayed(const Duration(milliseconds: 300));
  }
}
